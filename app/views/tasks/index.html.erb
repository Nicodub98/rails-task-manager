<h1> All our tasks</h1>
 <%= link_to "Add a new task", task_path %>
<ul>
  <% @tasks.each do |task| %>
    <li><%= task.title %></li>
  <%= link_to "Go see", task_path(task) %>
   <%= link_to "Delete", task_path(task), method: :delete %>
  <% end %>
</ul>
